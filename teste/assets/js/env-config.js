/* =========================================================================
   tpc32 — env-config.js (PLACEHOLDER dev)
   Em produção este arquivo é SOBRESCRITO pelo build command de cada projeto
   Cloudflare Pages (somente branch main), a partir das variáveis SITE_KEY e
   GTM_ID do projeto: org / meta / gg — 1 deploy → 1 identidade → 1 container.
   Em dev local e previews de branch, este placeholder permanece: sem GTM.
   NÃO colocar IDs reais aqui. Ver docs/superpowers/specs/2026-07-10-*.md.
   ========================================================================= */
window.SITE_CONFIG = { key: "dev", gtm_id: "", redirect: "" };
